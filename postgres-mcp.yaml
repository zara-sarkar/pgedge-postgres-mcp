databases:
  - name: "makeup_db"
    host: "127.0.0.1"
    port: 5432
    database: "makeup"
    user: "zsarkar01"
    password: ""
    sslmode: "disable"
    allow_writes: true

http:
  enabled: true
  address: ":8080"
  auth:
    enabled: false # Perfect! Authentication is bypassed
    token_file: "./pgedge-postgres-mcp-tokens.yaml"

user_file: "./pgedge-postgres-mcp-users.yaml"

services:
  llm:
    provider: "ollama"
    model: "qwen2.5:3b" # Updated to your new tiny model!
    url: "http://127.0.0.1:11434"
  embedding:
    enabled: true
    provider: "ollama"
    model: "all-minilm" 
    url: "http://127.0.0.1:11434"

knowledgebase:
  enabled: true
  embedding_provider: "ollama"
  embedding_model: "all-minilm" 
  embedding_ollama_url: "http://127.0.0.1:11434"

llm:
  enabled: true
  provider: "ollama"
  model: "qwen2.5:3b" # Updated to your new tiny model!
  ollama_url: "http://127.0.0.1:11434"